<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:KJK.App.Views"
             xmlns:vm="clr-namespace:KJK.App.ViewModels"
             x:Class="KJK.App.Views.MainPage"
             NavigationPage.HasNavigationBar="False"
             >
    <ContentPage.BindingContext>
        <vm:MainVM x:Name="VM"/>
    </ContentPage.BindingContext>

    <StackLayout>
        <Image Source="Logo.png"/>
        <Button Text="New Adventure" Command="{Binding cmdNewAdventure}"/>
        <Button Text="Continue" Command="{Binding cmdContinue}"/>
        <Button Text="Settings" Command="{Binding cmdSettings}"/>
    </StackLayout>

</ContentPage>
